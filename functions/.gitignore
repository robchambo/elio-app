# Compiled JavaScript files + test fixtures copied at build time
lib/

# Dependencies
node_modules/

# Logs
*.log
firebase-debug.log
ui-debug.log

# Local env (never commit — Function secrets live in Firebase Secret Manager)
.env
.env.local
.runtimeconfig.json
